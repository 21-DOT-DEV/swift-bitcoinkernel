<!--
Delete this comment before committing.

The evidence layer — add this file when decisions rest on evidence from
outside this repository: upstream source behavior, platform guarantees. Rules:

  * No frontmatter, no `status` field — plan.md alone carries those.
  * One numbered section per decision area: what was chosen, why, and what else
    was evaluated with why it lost. Rejected designs are recorded so they are
    not re-attempted.
  * Cite sources — vendored file paths, upstream issues, platform
    documentation. Cite upstream code by symbol name, not file:line — line
    numbers churn under patches and version bumps. Flag anything unverifiable
    locally as a device question, and mark genuinely undecided items
    `Unresolved:` so they cannot be mistaken for settled decisions.
-->

# Research — NNN · <title>

Evidence and rejected alternatives for [the plan](./plan.md). Spec in
[spec.md](./spec.md); ordered work in [tasks.md](./tasks.md).

## 1. <decision area>

**<Decision taken>.** <Rationale.> Alternatives considered: <what else was
evaluated and why each lost>.
