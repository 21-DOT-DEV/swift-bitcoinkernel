StylesPath = .vale/styles
MinAlertLevel = error

# Scoped to plans and decision records only. Roadmap phase files legitimately
# carry a Change Log section, so they are deliberately out of scope.
[Development/{Specs,ADRs}/**/*.md]
BasedOnStyles = Plans
